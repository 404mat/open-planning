{
  "name": "poker-planning-service",
  "private": true,
  "scripts": {
    "dev:client": "pnpm --filter client dev",
    "dev:server": "pnpm --filter server dev",
    "dev": "concurrently \"pnpm dev:client\" \"pnpm dev:server\"",
    "build": "pnpm -r build",
    "start": "pnpm -r start",
    "install:all": "pnpm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
